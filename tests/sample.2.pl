class Arithmetic {
  Void main(Float x, Float y, Float r, Float theta) {
    Float epsilon;
    epsilon = s0M3p051t1v3REALNUMBERgt0;
    return abs(r * (cos(theta) + i * sin(theta)) - (x + i * y)) < this.epsilon;
  }
}
