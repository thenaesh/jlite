class Main {
    Void main(Int x, Int y, Int z) {
        Int w;
        Bool boo;
        Int area;
        Box b;
        Square sq;
        String str;
        boo = !true;
        b = new Box();
        sq = new Square();
        b.x = 10 + 11;
        b.y = -(3 - b.z);
        b.z = 135;
        while (w >= 0) {
            println("abc");
            readln(str);
            if (w > 1024) {
                return;
                w = w / 2;
            } else {
                w = w - 1;
            }
        }
        area = sq.area();
        w = b.mass(100);
    }
}

class Box {
    Int x;
    Int y;
    Int z;
    Box b;
    Int vol() {
        Square unnecessaryVar;
        if (this.x < 0) {
            this.b.x = -this.x;
            return this.b.vol();
        } else {
            return this.x * this.y * this.z;
        }
    }
    Int mass(Int density) {
        return this.vol() * density;
    }
}

class Square {
    Int x;
    Int y;
    Bool z;
    Bool w;
    Int area() {
        return this.x * this.y;
    }
}